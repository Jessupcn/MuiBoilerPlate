import { Typography } from '@material-ui/core';

// Local Imports
import Routes from './Routes';

const App = () => {
  return (
    <div className="App">
      <Typography>Material-UI Boilerplate</Typography>
      <Routes />
    </div>
  );
};

export default App;
