/**
 * MuiTheme Processor:
 * Takes the createMuiTheme object from theme.js
 * and appends per-component styles to the overrides object
 */

// import the Mui theme
import { theme } from './theme';

// import the overrides
// import { MuiButton } from './muiButton';

// apply the overrides
// theme.overrides.MuiButton = MuiButton;

// export the completed theme
export { theme }
